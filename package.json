{
  "name": "future-intern",
  "version": "1.0.0",
  "description": "A comprehensive internship portal with modern UI and full-stack functionality",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": [
    "internship",
    "portal",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "Future Intern Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
